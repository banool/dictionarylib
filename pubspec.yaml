name: dictionarylib
description: "Library for the sign language dictionary apps"
version: 0.2.14

publish_to: 'none'

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.4.1
  carousel_slider:
  collection:
  cupertino_icons:
  device_info_plus:
  dolphinsr_dart:
    git:
      url: https://github.com/banool/dolphhinsr_dart.git
      ref: master
  dots_indicator:
  edit_distance: ^0.5.0-nullsafety.0
  flutter:
    sdk: flutter
  flutter_cache_manager:
  flutter_localizations:
    sdk: flutter
  flutter_markdown: ^0.7.4+1
  flutter_native_splash: 2.4.2
  go_router: ^14.3.0
  http:
  http_proxy:
    git:
      url: https://github.com/banool/http_proxy
      ref: master
  intl:
  json_annotation: ^4.8.1
  json_serializable: ^6.7.0
  mailto:
  multi_select_flutter:
    git:
      url: https://github.com/erickgon92/multi_select_flutter
      ref: master
  package_info_plus:
  path_provider:
  settings_ui:
  shared_preferences:
  store_redirect:
  url_launcher:
  video_player:

dev_dependencies:
  build_runner: ^2.3.3
  flutter_driver:
    sdk: flutter
  flutter_launcher_icons:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints:

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  generate: true
